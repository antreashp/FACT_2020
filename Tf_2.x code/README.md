# TF 2.x version of ELDR

## Requirements
The packages used to make the code work can be found in 'requirements.txt'.

##Virtual environment
Or make use of the virtual environment to run the code. Start the virtual environment with:

`cd eldr-env/bin`
`source activate`
